# Build Project Workflow

Follow these steps to install dependencies, run type checking, and build the application for production:

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **TypeScript Type Check**:
   ```bash
   npm run typecheck
   ```

3. **Production Build**:
   ```bash
   npm run build
   ```
